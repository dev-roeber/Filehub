# Filehub Gateway (Caddy) - Beispiel-Variablen.
# Reale Werte gehoeren in das Repo-Root .env, NICHT hier hinein.

# Lokaler Host-Port fuer das Gateway (Default 3080).
FILEHUB_GATEWAY_PORT=3080

# Zeitzone (Default Europe/Berlin).
TZ=Europe/Berlin
