# Alle Services dieses Compose-File wurden ins modulare Layout migriert.
# Siehe infra/authentik/compose.yml (authentik-postgres, authentik-redis,
# authentik-server, authentik-worker) und infra/gateway/compose.yml
# (filehub-gateway). Diese Datei bleibt als Rollback-Reserve.
services: {}

networks:
  authentik_net:
    name: authentik_net
    driver: bridge
  filehub_net:
    name: filehub_net
